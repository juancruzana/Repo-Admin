# base de la API: en dev es relativa, el proxy de Vite la manda a localhost:8000
VITE_API_BASE_URL=/api
# WebSocket de pedidos en tiempo real (absoluta: el WS no pasa por el proxy)
VITE_WS_URL=ws://localhost:8000/api/v1/ws
